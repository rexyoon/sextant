# 프로젝트 기획서 — 《SECOND》 (가칭)

**개발자를 위한 AI 커리어 비서 · "나만의 AI 회사" 2호 직원**

| 항목 | 내용 |
|---|---|
| 프로젝트명 | SECOND (가칭) — 세컨드 브레인 겸 2호 직원. 후보: Chief, Compass, 書記 |
| 한 줄 정의 | 내 개발 활동을 자동으로 기록해 역량 프로필로 만들고, 채용시장과 대조해 "다음에 뭘 만들지"를 알려주는 AI 비서 |
| 실사용자 | 정재윤 본인 (1인 도그푸딩) |
| 세계관 | "나만의 AI 회사" 2호 직원 — 1호 파수꾼(인프라)에 이은 비서실장 |
| 성격 | 포트폴리오 + 실제 매일 쓰는 도구 |

---

## 1. 문제 정의

### 1.1 지금 겪고 있는 진짜 불편
1. **기록이 안 남는다** — 매일 코드를 쓰고 문제를 풀지만, 그 과정이 문서로 남지 않는다. 면접에서 "그때 어떻게 해결했냐"는 질문에 기억으로 답한다.
2. **역량 증명이 수동이다** — 이력서·포트폴리오를 지원할 때마다 손으로 고친다. 실제 한 일과 문서 사이에 시차가 생긴다.
3. **채용공고를 매번 다시 읽는다** — 열 곳 넘게 지원하면서 요구 스택을 매번 눈으로 파싱하고, 마감일을 따로 관리한다.
4. **면접 준비가 막연하다** — 무엇을 물어볼지 감으로 예상한다. 정작 내 코드에서 나올 질문을 놓친다.
5. **프롬프트 자산이 흩어진다** — 프로젝트마다 CLAUDE.md와 프롬프트 팩이 있는데 재사용·개선이 안 된다.

### 1.2 공통 원인
다섯 가지 불편이 전부 **"나에 대한 데이터가 흩어져 있어서"** 생긴다. git에, Claude Code 세션에, 이력서 파일에, 북마크에 따로 있다. 하나로 모으면 각각이 서로를 강화한다.

### 1.3 제품 논지 (이 서비스가 파는 것)
> **개발 활동 → 역량 근거 → 시장 매칭 → 다음 할 일**로 이어지는 닫힌 고리.
> 기록이 자동이므로 고리가 저절로 돈다.

```
   ┌──────────────────────────────────────────────┐
   │                                              │
   ▼                                              │
[코드를 쓴다] → [일지·트러블슈팅 자동 생성] → [역량 프로필 갱신]
                                                  │
                                                  ▼
[다음 프로젝트 결정] ← [갭 분석] ← [채용공고 매칭] ←┘
        │                                    │
        └──────────────→ [면접 예상 질문 생성] ┘
```

---

## 2. 기능 구성 (5개 에이전트, 1개 지식베이스)

### 2.0 공통 기반 — Knowledge Base (KB)
모든 에이전트가 읽고 쓰는 단일 저장소.
- **활동 원본**: git 커밋/diff 요약, 작업 세션 메모, 해결한 에러 로그
- **파생 자산**: 개발 일지, 트러블슈팅 문서, 역량 프로필
- **외부 데이터**: 채용공고, 지원 현황
- 벡터 검색(pgvector)으로 "내가 전에 이 문제 어떻게 풀었지?" 질의 가능

### 2.1 📝 기록자 (DevLog Agent)
- 입력: git 로그·diff, 로컬 CLI로 남긴 메모, 에러 로그
- 출력: **일일/주간 개발 일지**, **트러블슈팅 문서**(증상→가설→원인→해결→배운 것)
- 트리거: 수동 커맨드(`second log`) + 일 1회 배치
- 핵심: 커밋 메시지만으로는 부족하므로, diff와 함께 LLM이 "무엇을 왜 바꿨는지" 추론하고 사람이 한 줄 보완

### 2.2 🧬 프로필러 (Profile Agent)
- 입력: KB의 활동·일지
- 출력: **근거 기반 역량 프로필** — "Spring Boot(중급): 근거 12건 (KoALa 동시성 처리, 파수꾼 API…)"
- 핵심 차별점: **자기 신고가 아니라 커밋으로 증명되는 스킬**. 이게 이력서와의 결정적 차이.
- 부가: 이력서/포트폴리오 초안 자동 생성(지원 회사별 강조점 조정)

### 2.3 🎯 스카우터 (Job Agent · 舊 StackRadar)
- 채용공고 수집 → LLM이 요구 스택·경력·우대사항을 **구조화 파싱**
- 내 프로필과 **매칭 점수** + **갭 리스트** 산출
- 지원 현황 추적(관심/지원/서류통과/면접/결과), 마감일 알림
- ⚠️ 수집은 §6 준수 필수(공식 API/RSS 우선, robots.txt 존중, 개인 사용 규모)

### 2.4 🎤 면접관 (Interview Agent)
- 입력: 내 코드베이스 + 지원한 공고 + KB
- 출력: **내 코드에서 나올 질문** ("이 예약 로직에서 비관적 락을 쓴 이유는?"), 모범 답변 초안, 간격 반복(spaced repetition) 복습 큐
- 핵심: 일반 CS 질문집이 아니라 **내 실제 코드 기반**이라는 점

### 2.5 🧰 사서 (Prompt Agent)
- CLAUDE.md·프롬프트 팩을 프로젝트별로 버전 관리
- 공통 규칙(작업 방식, 금지사항)을 상속받아 프로젝트별 constitution 생성
- 어떤 프롬프트가 잘 먹혔는지 기록 → 개선

---

## 3. 기술 스택 & 아키텍처

### 3.1 스택
| 영역 | 선택 | 이유 |
|---|---|---|
| API 서버 | Kotlin + Spring Boot 3 | 기존 Java/Spring 강점 + Kotlin 학습(채용 수요) |
| 에이전트 워커 | Python + FastAPI | LLM 오케스트레이션·수집·파싱 생태계 |
| DB | PostgreSQL + **pgvector** | 관계형 + 벡터 검색 한 곳에서. RAG 학습 |
| 캐시/큐 | Redis | 작업 큐, 중복 방지, LLM 응답 캐시 |
| 프론트 | Next.js + TypeScript | 대시보드 |
| CLI | Kotlin Native 또는 Python | `second log`, `second ask` — 터미널에서 바로 |
| LLM | Claude API | 구조화 출력(JSON 스키마) 강제 |
| 배포 | Docker Compose + EC2/Oracle Cloud | 기존 경험 재활용 |

### 3.2 구조
```
[로컬 CLI] ──git 로그·메모──┐
                            ▼
                    [Spring Boot API]
                      │        │
                      │        └──작업 큐(Redis)──▶ [Python Agent Worker]
                      │                               ├─ LLM 호출(Claude)
                      │                               ├─ 공고 수집·파싱
                      ▼                               └─ 임베딩 생성
              [PostgreSQL + pgvector]  ◀──────────────┘
                      ▲
                      │
              [Next.js 대시보드]
```

### 3.3 핵심 설계 결정
| 결정 | 선택 | 트레이드오프 |
|---|---|---|
| 에이전트 실행 | 동기 API가 아닌 **큐 기반 비동기** | LLM 호출이 느리고 실패하므로. 재시도·부분 실패 처리 학습 |
| LLM 출력 | JSON 스키마 강제 + 검증 실패 시 재시도 1회 | 자유 텍스트는 파이프라인에서 못 쓴다 |
| 코드 전송 범위 | **diff 요약만**, 전체 소스 미전송 | 프라이버시·비용. §6 참조 |
| 사람의 개입 | 자동 생성 후 **반드시 사람이 승인/수정** | 자동 생성물을 그대로 포폴에 쓰면 신뢰가 깨진다 |
| 멀티에이전트 | 오케스트레이터 없이 **독립 에이전트 + 공유 KB** (초기) | 프레임워크 도입은 과설계. 필요해지면 추가 |

---

## 4. 데이터 모델 (초안)

```sql
-- 활동 원본
activity(id, type[COMMIT|NOTE|ERROR], project, ref, raw_text, occurred_at)

-- 파생 문서
document(id, type[DEVLOG|TROUBLESHOOT|RESUME], title, body_md,
         period_start, period_end, status[DRAFT|APPROVED], created_at)
document_source(document_id, activity_id)        -- 근거 추적

-- 역량 프로필
skill(id, name, category, level, updated_at)
skill_evidence(skill_id, activity_id, note)      -- 스킬의 '근거'

-- 채용
job_posting(id, company, title, url, source, deadline, raw_text, fetched_at)
job_requirement(id, job_posting_id, skill_name, importance[MUST|NICE])
application(id, job_posting_id, status, applied_at, memo)
match_result(job_posting_id, score, gap_json, computed_at)

-- 면접
question(id, source_type[CODE|JOB], source_ref, question, answer_draft,
         difficulty, next_review_at, ease)       -- 간격 반복용

-- 프롬프트
prompt_pack(id, project, version, content_md, created_at)

-- 벡터
embedding(id, target_type, target_id, vector)     -- pgvector
```

---

## 5. 개발 로드맵 (엄격한 단계 분리)

> **철칙: 각 Phase가 끝나면 "그것만으로도 매일 쓰는 도구"여야 한다.**
> 다음 Phase는 앞 Phase를 2주 이상 실제로 써본 뒤에 착수한다.
> 이 규칙을 어기면 통합형 프로젝트는 반드시 미완으로 끝난다.

### Phase 0 — 기반 (1주)
- 모노레포, Docker Compose(PG+pgvector, Redis), Spring/FastAPI 스캐폴드
- `activity` 테이블 + CLI로 수동 메모 남기기
- **완료 기준**: 터미널에서 `second note "오늘 X 했음"` 이 DB에 저장된다

### Phase 1 — 기록자 (2.5주) ★ 여기가 MVP
- git 로그·diff 수집기, LLM 일지 생성, 트러블슈팅 문서 생성
- 웹에서 초안 확인·수정·승인 → 마크다운 내보내기
- **완료 기준**: 2주간 매일 사용해 일지가 쌓이고, 트러블슈팅 문서 3건이 실제 포폴에 들어간다
- ⛔ 여기서 만족스럽지 않으면 다음으로 가지 말 것

### Phase 2 — 프로필러 (1.5주)
- 활동·일지에서 스킬 추출 + 근거 연결, 레벨 산정 규칙
- 역량 프로필 화면, 이력서 초안 생성
- **완료 기준**: "Spring Boot 근거 N건" 이 실제 커밋으로 클릭 추적된다

### Phase 3 — 스카우터 (2.5주)
- 공고 수집(§6 준수), LLM 구조화 파싱, 매칭·갭 분석
- 지원 현황 보드, 마감 알림
- **완료 기준**: 실제 지원에 이 도구를 써서 갭 하나를 발견하고 대응한다

### Phase 4 — 면접관 (1.5주)
- 코드 기반 질문 생성, 답변 초안, 간격 반복 복습
- **완료 기준**: 실제 면접에서 이 도구로 준비한 질문이 나온다(검증!)

### Phase 5 — 사서 + 통합 (1.5주)
- 프롬프트 팩 버전 관리, 대시보드 통합(고리 시각화)
- 배포, README, 데모 영상

**총 약 10.5주** — 단, Phase 1만으로도 독립 포폴로 성립한다.

---

## 6. ⚠️ 리스크 & 준수사항 (반드시 읽을 것)

### 6.1 채용공고 수집의 법적 경계
- **공식 API·RSS·오픈 데이터를 최우선**으로. 없는 곳은 수집 대상에서 제외하는 것이 안전
- robots.txt 및 각 사이트 이용약관 확인. 금지된 곳은 크롤링하지 않는다
- 요청 간격 제한(rate limit), 개인 사용 규모 유지
- 수집 데이터를 **재배포·공개 서비스화하지 않는다** (개인 도구로 한정)
- 포폴 공개 시 이 준수 원칙을 README에 명시 → 오히려 신뢰 요소가 된다

### 6.2 코드·데이터 프라이버시
- 회사 업무 코드(SI 프로젝트)는 **절대 KB에 넣지 않는다**. 개인 레포만 대상
- LLM에는 전체 소스가 아닌 **diff 요약**만 전송. 시크릿 패턴 필터링 선행
- KB에 API 키·비밀번호가 섞이지 않도록 정규식 스캐너 필수

### 6.3 자동 생성물의 신뢰
- LLM이 쓴 일지·이력서를 **검토 없이 사용하지 않는다**. 사실과 다른 서술은 면접에서 치명적
- 모든 문서에 `status(DRAFT/APPROVED)` — 승인된 것만 내보내기
- 스킬 레벨은 자동 산정하되 **근거를 항상 함께 표시** (근거 없는 주장 금지)

### 6.4 스코프 리스크
- 통합형 프로젝트의 사망 원인 1위. §5의 "Phase 완료 후 2주 사용" 규칙이 유일한 방어선
- 취업 준비와 병행이므로, **Phase 1 완료 시점을 1차 목표**로 삼고 나머지는 여유에 따라

### 6.5 비용
- LLM 호출을 일 배치 + 캐싱으로 제한. 월 한도 설정
- 임베딩은 변경분만 재생성

---

## 7. 포트폴리오 관점의 강점

1. **실사용자가 확실한 제품** — 개발자 본인이 매일 쓴다. 요구사항이 진짜다.
2. **AI 파이프라인 실전** — 프롬프트 설계, 구조화 출력, 재시도, 비용 통제, RAG. 지금 채용시장의 최대 관심사.
3. **비동기 작업 설계** — 큐, 워커, 부분 실패 처리. 백엔드 실력의 증명.
4. **메타 구조의 서사** — "포트폴리오를 만들어주는 도구를 포트폴리오로 낸다". 면접관이 기억한다.
5. **세계관 연결** — 1호 파수꾼(인프라) → 2호 세컨드(비서). "AI 회사를 만들고 있다"는 일관된 서사.

---

## 8. 다음에 정할 것
1. 이름 확정 (SECOND / Chief / Compass / 書記 …)
2. 백엔드를 Kotlin으로 갈지 Java로 갈지 (학습 vs 속도)
3. Phase 1의 일지 생성 주기: 커밋 훅 실시간 vs 일 1회 배치
4. 공고 수집 대상 사이트 목록 — §6.1 기준으로 화이트리스트 확정
5. 대시보드를 웹으로 할지, 초기엔 CLI + 마크다운 파일로 갈지 (더 가볍게)
