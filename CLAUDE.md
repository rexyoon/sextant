# SEXTANT — 개발자 AI 커리어 비서

## 프로젝트 정체
내 개발 활동(git 커밋·메모·에러)을 자동으로 기록해 개발 일지와 트러블슈팅 문서를
만들고, 이를 역량 프로필 → 채용 매칭 → 갭 분석으로 잇는 개인용 도구.
"나만의 AI 회사" 2호 직원. (1호는 Sentinel — 서버 모니터링 시스템)

이름의 뜻: 육분의(sextant)는 별을 보고 내 위치를 계산하는 도구.
채용시장을 관측해 내 좌표를 알려준다는 뜻.

## 지금의 스코프 (엄수)
Phase 1까지만 만든다. **CLI + 마크다운 출력**이 전부다.
웹 대시보드, 채용공고 수집, 면접 질문 생성은 **이번 범위가 아니다.**
요청하지 않은 기능을 추가하지 마라.

## 기술 스택 (Phase 1)
- Python 3.11 + Typer (CLI) — 이 머신에서는 `py -3.11` 로 호출한다
- SQLite (로컬 단일 파일 — Postgres는 Phase 2에서 이관)
- Claude API (구조화 출력, JSON 스키마 강제)
- 출력: 마크다운 파일 (`~/.sextant/logs/`)
- 패키지: venv + pip (uv 미설치)

> Phase 2에서 Kotlin/Spring API + Postgres+pgvector + Next.js로 확장.
> 그래서 지금도 **저장 계층을 인터페이스로 추상화**해 이관이 쉽게 한다.

## 멀티 프로젝트 (필수 설계)
- 아무 레포 디렉토리에서 실행하면 현재 git 레포를 자동 인식
- 레포 경로 → project 식별자 매핑을 설정 파일에 저장
- 대상 레포는 언어 구성이 제각각이다(Java/Spring, Python, TS/React).
  일지 프롬프트는 특정 언어를 가정하지 말고 실제 diff에서 판별한다.
- 첫 타깃 레포: **미정 (결정 후 이 줄을 갱신할 것)**

## 인코딩 (이 머신 한정 함정)
기본 인코딩이 **cp949** 다. 출력은 `cli._force_utf8_output()` 가 진입 시 UTF-8로 고정한다.
파일을 열 때는 **항상 `encoding="utf-8"` 을 명시**하라. 생략하면 cp949로 열려서
남의 레포에서 온 문자에 `UnicodeEncodeError` 가 난다.

## 작업 방식
1. 코드 변경 시 **파일 전체 출력**. 부분 스니펫 금지.
2. 구현 전 "무엇을/왜/어떻게" 3줄 계획 먼저.
3. LLM 호출부는 프롬프트도 코드 리뷰 대상 — 초안을 보여주고 승인받아라.
4. 학습 목적 병행: 핵심 로직에 "왜 이렇게"를 주석 + 작업 후 3줄 요약.
5. 확인이 필요하면 질문 1개.

## 보안 (절대 규칙)
- **회사 업무 코드(SI 프로젝트)는 대상에서 제외.** 개인 레포만.
- LLM에 전체 소스를 보내지 않는다. **diff 요약만** 전송.
- 전송 전 **시크릿 스캐너**를 반드시 통과: API 키, 비밀번호, 토큰, .env 내용,
  private key 패턴을 정규식으로 검출해 마스킹. 검출 시 해당 hunk 제외.
- API 키는 환경변수. 코드·git 커밋 금지.

## 자동 생성물의 신뢰
- LLM이 쓴 문서는 전부 status=DRAFT. 사람이 승인해야 APPROVED.
- 사실과 다른 서술은 면접에서 치명적 → 승인 없이 내보내기 금지.
- 문서마다 근거가 된 커밋 해시를 반드시 남긴다(추적 가능성).

## 브랜치
1인 프로젝트다. **main 에서 바로 작업한다.**
브랜치는 **내가 명시적으로 요청했을 때만** 만든다. 알아서 파지 마라.

## 하지 말 것
- 스코프 크리프 (웹/채용/면접 기능 선반영)
- 전체 소스 코드를 LLM에 전송
- 시크릿 스캐너 우회
- 요청하지 않은 브랜치 생성
